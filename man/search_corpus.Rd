% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_corpus.R
\name{search_corpus}
\alias{search_corpus}
\title{Search a text corpus for patterns (regex), with optional KWIC-style context.}
\usage{
search_corpus(
  corpus,
  by = c("doc_id", "paragraph_id", "sentence_id"),
  search,
  context_size = 0,
  highlight = c("<b>", "</b>"),
  cores = 1
)
}
\arguments{
\item{corpus}{A data frame or data.table with \code{text} and id columns given in \code{by}.}

\item{by}{Character vector of columns defining text units (e.g. \code{c("doc_id", "sentence_id")}).}

\item{search}{The search pattern or query (regex; multiple patterns as vector are OR'd).}

\item{context_size}{Numeric, default 0. Context size in sentences around matches.}

\item{highlight}{Length-two character vector for match highlighting (default \code{c('<b>', '</b>')}).}

\item{cores}{Numeric, default 1. Number of cores for parallel processing.}
}
\value{
A data.table with \code{by} columns, \code{text}, \code{start}, \code{end}, \code{pattern}.
}
\description{
Searches a text corpus for specified patterns, with support for parallel processing.
}
\examples{
corpus <- data.frame(doc_id = c('1', '1', '2'),
                    sentence_id = c('1', '2', '1'),
                    text = c("Hello world.",
                             "This is an example.",
                             "This is a party!"))
search_corpus(corpus, search = 'This is', by = c('doc_id', 'sentence_id'))
}
