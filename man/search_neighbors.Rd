% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_vector.R
\name{search_neighbors}
\alias{search_neighbors}
\title{Nearest neighbors by cosine similarity (vector search)}
\usage{
search_neighbors(x, matrix, n = 10)
}
\arguments{
\item{x}{A character (row name in \code{matrix}) or numeric vector (query embedding).}

\item{matrix}{A numeric or sparse matrix of embeddings (rows = documents/units).}

\item{n}{Number of nearest neighbors to return (default 10).}
}
\value{
A data frame with \code{rank}, \code{term1}, \code{term2}, \code{cos_sim}.
}
\description{
Returns the top-\code{n} nearest rows in an embedding matrix to a query
(row name or numeric vector). Local search over a dense/sparse matrix you
provide. textpress does not generate embeddings; use packages such as
\pkg{reticulate} (sentence-transformers), \pkg{word2vec}, or \pkg{fastText}
to create the matrix, then pass it here.
}
\examples{
\dontrun{
# Assume you have an embedding matrix from another package
# m <- your_embed_function(corpus)
# search_neighbors("doc_1", matrix = m, n = 5)
}
}
